# Riverbraid-Lang
**Relational Substrate**

A substrate for AI-driven relational intelligence. By grafting Riverbraid-Core onto the language chain, we establish a "Veto Layer" that prevents output generation if the underlying logic has been compromised.

### **Verification**
`node -e "require('./riverbraid-core/run-vectors.cjs').verify()"`
> Validates the stationary floor of the relational engine.
